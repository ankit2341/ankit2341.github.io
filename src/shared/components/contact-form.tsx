import { Box, VStack } from '@chakra-ui/react';

const ContactForm = () => {
  return (
    <VStack w={'100%'} spaceY={2} flex={1}>
      <Box flex={0.5} bg={'yellow.900'}>
        
      </Box>
      <VStack flex={0.5} bg={'blue.900'}>
        csdvfb
      </VStack>
    </VStack>
  );
};

export default ContactForm;
