/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/ankit2341.github.io', // replace with your actual repo name
  assetPrefix: '/ankit2341.github.io/',
};

export default nextConfig;
